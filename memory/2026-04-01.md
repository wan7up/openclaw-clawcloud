# 2026-04-01

- 用户正式确认两条构建包基线版本（以 GHCR 上实际可用包为准）：
  1. 任务 001 / ClawCloud Run 线：`ghcr.io/wan7up/openclaw-clawcloud:v0.1.12` 是当前可用基线版本。
  2. 任务 004 / ARM64 线：`ghcr.io/wan7up/openclaw-arm64:2026.3.24-manual-devices-v8` 是当前可用基线版本。
- 后续关于“自动跟随 OpenClaw 上游版本更新”的方案设计、脚本、发布逻辑，都应以上述两个 GHCR 基线 tag 为起点，而不是继续沿用旧记忆里的 `v0.1.8` 或更早测试 tag。
- 用户当前明确要求：任务 001 与任务 004 的两个构建包，都要根据 OpenClaw 新版本自动更新，并在 OpenClaw 发布新版后的第二天完成相应更新。
- 已完成本轮第一、二层自动化骨架：
  1. `scripts/sync_openclaw_package_bases.py`：自动读取 OpenClaw 最新 release，并推进任务 001 / 004 的基底版本。
  2. `.github/workflows/sync-openclaw-package-bases.yml`：每日运行的“次日检查”入口。
  3. `.github/workflows/publish-openclaw-packages.yml`：自动 build + push 任务 001（ClawCloud Run）与任务 004（ARM64）到 GHCR。
  4. `scripts/make_arm_v8_descriptor.py`：任务 004 发布后自动将 ARM64 tag 改写为 `linux/arm/v8` manifest 元数据，以兼容较老 Docker / CoreELEC 环境。
- 当前约定的自动发布 tag 规则：
  - 任务 001：`ghcr.io/<owner>/openclaw-clawcloud:v<upstream_version>` + `latest`
  - 任务 004：`ghcr.io/<owner>/openclaw-arm64:<upstream_version>-manual-devices-v8` + `latest-manual-devices-v8`
- 结构决策：暂时保持“**单仓双包、逻辑双线**”——同一 GitHub 仓库 `wan7up/openclaw-clawcloud` 内维护两条打包线，但 GHCR 包分为 `openclaw-clawcloud`（任务 001 / ClawCloud Run）与 `openclaw-arm64`（任务 004 / ARM64）。后续我在读记忆、写文档、更新任务状态时，必须把这两条线视为不同交付物，不能因为共仓就混为一谈。
- ARM64 CLI 超时定位结果：在问题机器上执行 `OPENCLAW_NO_RESPAWN=1 openclaw gateway status --no-probe` 可立即正常返回并 exit code 0，说明问题核心落在 upstream CLI `entry.js` 的 respawn/bootstrap 链，而不是 gateway status 业务实现本身。为避免旧坏包与新修复包混淆，ARM64 自动发布 tag 从 `manual-devices-v8` 升为 `manual-devices-v9`，并在 ARM64 镜像默认注入 `OPENCLAW_NO_RESPAWN=1` 作为当前缓解策略。
