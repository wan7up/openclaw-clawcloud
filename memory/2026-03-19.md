# 2026-03-19 Daily Notes

- 为 OpenClaw 跨服务器备份任务（任务 008）打通了 `rclone -> OpenList(WebDAV)` 远端备份链路；remote 名为 `ocbackup`，目标目录为 `ocbackup:/openclaw-migration/`。
- 已确认 OpenList 权限上仅 `WebDAV 读取` 不足以支持 rclone 写入；补上相关写权限后，rclone 的 mkdir / upload 正常工作。
- `backup/backup-openclaw-migration.sh` 已扩展为：生成迁移备份包、生成 `.sha256`、同步 `README.md`、自动上传到 OpenList，并执行本地/远端保留策略。
- 当前迁移备份保留策略：本地最近 2 份，远端最近 2 份。
- 已补 `backup/OPENCLAW_BACKUP_README.md`，并同步到本地与远端备份目录，内容包含手动备份、校验、恢复流程说明。
- 已新增 `backup/restore-openclaw-migration.sh` 恢复脚本，支持：从远端拉取最新或指定备份、`--check-only` 校验、`--yes` 直接恢复到 `/`。
- 已实际验证：迁移备份脚本可成功生成并上传；恢复脚本 `--from-remote --check-only` 校验通过。
- 任务 008 当前状态：核心链路已落地（备份、上传、保留、说明、恢复脚本都已完成）；剩余可选项是未来做一次真实新机恢复演练。
- 用户明确说明：任务 008 还有后续，后面会再增加一个备份位置；同时还需要把“回滚备份”也同步到远端，但因为回滚备份体积更大，需要优先考虑流量/带宽成本，暂不急着直接上线。
