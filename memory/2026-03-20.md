# 2026-03-20 Daily Notes

- ClawCloud Run task 001 advanced to build/push stage. Built image `ghcr.io/wan7up/openclaw-clawcloud:latest`, but GHCR push failed due to token missing `write:packages` scope. User will generate a **classic** GitHub token with `write:packages` + `read:packages` (and `repo` only if private) to continue.
- GHCR selected as registry. GitHub repo renamed to `wan7up/openclaw-clawcloud`.
- User later provided a GitHub classic token with `write:packages`/`read:packages`; GHCR login and push then succeeded. Published images include `ghcr.io/wan7up/openclaw-clawcloud:latest`, plus version tags `v0.1.1` and `v0.1.2` to avoid stale-cache pulls on ClawCloud Run.
- ClawCloud Run deployment hit three concrete prototype bugs that were fixed iteratively: (1) `configure.js` was treated as ESM because upstream image has `type: module`, so it was renamed to `configure.cjs`; (2) `OPENAI_BASE_URL` support was added to the generated config via `config.env`; (3) entrypoint path `/opt/openclaw/app` was wrong for the upstream image and was corrected to `/app` with `node /app/openclaw.mjs gateway run`.
- GHCR package visibility could not be switched to public via the attempted GitHub CLI/API flow (visibility PATCH returned 404 even though package read worked); user manually changed the package to Public in GitHub Web UI so ClawCloud Run could pull the image.
- ClawCloud Run deployment for task 001 successfully reached the state where the container starts and writes `/data/.openclaw/openclaw.json`; next validation steps are `/healthz`, root WebUI load, a simple chat test like `hello`, and persistence checks across restart.
- Telegram cross-surface sync check at 2026-03-20 06:54 SGT: Telegram session content was visible. No new durable preferences/decisions/todos were found; only a successful scheduled migration backup run was worth noting. Backup archive `openclaw-migration-2026-03-20-040008.tar.gz` and checksum `openclaw-migration-2026-03-20-040008.sha256` were created under `/root/backups/openclaw-migration`.
