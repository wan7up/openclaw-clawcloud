# 2026-03-14

- Established cross-surface memory policy for WebUI and Telegram.
- Scheduled a 6-hour sync job to attempt syncing durable memory items from Telegram-side visible sessions into shared memory.
- User requested an immediate-write rule: when they say "记住这个" or "这条写进记忆" on either WebUI or Telegram, write the item into shared memory immediately instead of waiting for scheduled sync.
- Confirmed `TASKS.md` exists in the shared workspace and contains active project/task items, including cross-claw memory sync and Telegram/Feishu user-ID allowlist bot routing.
- Investigated WebUI behavior via local OpenClaw docs: WebUI/WebChat uses gateway `chat.history`, which is bounded for UI safety; session/transcript storage exists on the gateway, but the visible history in WebUI is not guaranteed to show older content after reload.
- Discussed practical workflow implications: for long-running or file-heavy work, Telegram is likely the better primary surface; WebUI is better for continuous text discussion while the page remains open.
- Added a new convention: if the user says "收尾", perform a lightweight structured wrap-up with topic, key conclusions, next steps, and write durable items into shared memory/project files.
