# 2026-03-14

- Established cross-surface memory policy for WebUI and Telegram.
- Scheduled a 6-hour sync job to attempt syncing durable memory items from Telegram-side visible sessions into shared memory.
- User requested an immediate-write rule: when they say "记住这个" or "这条写进记忆" on either WebUI or Telegram, write the item into shared memory immediately instead of waiting for scheduled sync.
- Confirmed `TASKS.md` exists in the shared workspace and contains active project/task items, including cross-claw memory sync and Telegram/Feishu user-ID allowlist bot routing.
- Investigated WebUI behavior via local OpenClaw docs: WebUI/WebChat uses gateway `chat.history`, which is bounded for UI safety; session/transcript storage exists on the gateway, but the visible history in WebUI is not guaranteed to show older content after reload.
- Discussed practical workflow implications: for long-running or file-heavy work, Telegram is likely the better primary surface; WebUI is better for continuous text discussion while the page remains open.
- Added a new convention: if the user says "收尾", perform a lightweight structured wrap-up with topic, key conclusions, next steps, and write durable items into shared memory/project files.
- For task 006 (Feishu bot / allowlist multi-user routing), the implementation mainline changed during this session: instead of continuing a hand-built webhook bot, the correct current path is the official Feishu plugin flow (`@larksuite/openclaw-lark-tools install`) plus `/feishu start` and `/feishu auth` in Feishu chat.
- Verified that reading a private Feishu/Lark cloud document works after Feishu-side authorization using `feishu_fetch_doc`; the earlier failure was due to missing user authorization, not because the document was unreadable.
- Checked the workspace for conflicts: no active conflict or running custom Feishu bot process remained; the local `feishu-bot/` directory is now only a backup/experimental path and should not be treated as the current mainline.
- Scheduled 6-hour cross-surface memory sync ran at 2026-03-14 18:54 GMT+8; Telegram-side conversation content was still not visible from OpenClaw session listings, so no Telegram durable items were imported during this sync.
- Discussed the product shape for sharing OpenClaw with friends/enterprise users over Feishu. Key conclusion: the best core experience remains WebUI/Telegram, while Feishu is best positioned as an enterprise/private-work entry point rather than the sole long-form assistant surface.
- Clarified multi-user Feishu deployment model: model/provider billing can be centralized on the owner's OpenClaw/OpenAI account, but Feishu personal-data access should be per-user when needed (their own calendar/docs/tasks/messages should come from their own Feishu authorization, not the owner's account).
- Confirmed a Feishu user can join multiple enterprises, so friends can join the user's enterprise without needing separate standalone Feishu identities.
- Inspected current OpenClaw config and found the main blocker for multi-user Feishu DM access was `channels.feishu.dmPolicy=allowlist` with only the owner's open_id in `allowFrom`.
- Updated OpenClaw Feishu channel config for the new rollout plan: `channels.feishu.dmPolicy` changed to `open` so enterprise users who can access the app can DM the bot directly; `channels.feishu.groupPolicy` changed to `disabled` so group chats are ignored for now. Current intended policy is: enterprise-internal users use Feishu DMs as the main entry; groups are disabled; friends must still be added to the enterprise and the app must be visible to them on the Feishu side.
- Clarified Feishu `pairing`: it means first-contact approval by the operator (typically via OpenClaw CLI/control-side approval), not an in-Feishu one-click approval flow. Because the user wants low-friction enterprise use, `open` was chosen instead of `pairing` for Feishu DMs.
- Task 008 status update: backup/recovery plan and starter script have been prepared in `backup/OPENCLAW_BACKUP_PLAN.md` and `backup/backup-openclaw.sh`; implementation is pending when the user is ready to configure the remote backup target.
- Task 006 addendum: for multi-user access, Telegram and Feishu should be treated differently. Telegram usually does not have a second-layer user-authorization problem like Feishu; its main restriction is access control (allowlist/pairing/open), while Feishu may additionally be blocked by enterprise/user-authorization scope approval.
