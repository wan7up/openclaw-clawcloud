# OpenClaw ARM64 image workdir

This directory is intentionally separate from `deploy/clawcloudrun-openclaw/`.

Purpose:
- build and test a plain ARM64-oriented OpenClaw image flow
- avoid mixing this work with the ClawCloud Run adaptation layer
- keep Dockerfile / scripts / notes for ARM64 packaging isolated

Status:
- initialized on 2026-03-27
- task boundary explicitly separated from ClawCloud Run packaging
